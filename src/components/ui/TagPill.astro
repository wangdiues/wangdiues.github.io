---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-flex items-center rounded-full border border-[var(--color-border-subtle)] bg-transparent px-3 py-1.5 text-xs font-medium text-[var(--color-text-muted)]"
>
  {label}
</span>
