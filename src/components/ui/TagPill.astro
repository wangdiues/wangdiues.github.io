---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="glass-panel glass-panel-sm inline-flex items-center rounded-full px-3 py-1.5 text-xs font-medium text-[var(--color-text-muted)]"
>
  {label}
</span>
