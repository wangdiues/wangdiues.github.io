---
interface Props {
  status: 'published' | 'under-review' | 'in-preparation' | 'report';
  label?: string;
}

const { status, label } = Astro.props;

const styles: Record<Props['status'], string> = {
  published: 'border-moss-400/30 bg-moss-400/15 font-semibold text-moss-300',
  'under-review': 'border-amber-500/45 bg-amber-500/10 text-amber-500',
  'in-preparation': 'border-slate-400/35 bg-paper-50/[0.03] text-slate-400',
  report: 'border-paper-50/25 bg-paper-50/[0.05] text-paper-50',
};

const labels: Record<Props['status'], string> = {
  published: 'Published',
  'under-review': 'Under review',
  'in-preparation': 'In preparation',
  report: 'Report',
};
---

<span
  class:list={[
    'inline-flex items-center rounded-full border px-3 py-1.5 text-[0.68rem] font-medium tracking-[0.14em] uppercase',
    styles[status],
  ]}
>
  {label ?? labels[status]}
</span>
