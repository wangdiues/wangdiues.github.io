pages:
  stage: deploy
  script:
    - mkdir public
    - cp -r index.html *.css *.js content background_Videos public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
